# 文書作成に関する規約
本ドキュメントは，AIによる文書作成および学術文書の作成・改訂に共通して適用する規約を定める．
個別の論文や文書に固有の内容，研究上の仮定，実験条件，章構成等は，各指示書で定める．

---

# 1. 文書作成に関する共通規約

## 1.1 文書の目的
作成する文書は，指定された目的に応じて内容を構成し，個別の指示書に記載されていない事項についても，既存の文書やプログラムとの整合性を維持する．
文書を作成する際には，既存のファイル構成，関連文書，テンプレート等を確認し，既存の成果物と矛盾する内容を新たに作成しない．
既存のプログラム，設定ファイル，テンプレート等を変更する場合は，変更が必要な理由と既存機能への影響を確認する．
指示されていないファイルや設定を，不要な理由で変更しない．

## 1.2 既存文書の改訂
既存の文書を改訂する場合，原則として最新の版を基準として編集する．
改訂前の文書に含まれる内容を無条件に削除するのではなく，最新の指示との整合性を確認した上で必要な箇所のみを変更する．
複数の指示が存在する場合は，より新しい指示を優先する．

## 1.3 メタ情報の本文への混入禁止
プロンプトや作業指示そのものを，成果物の本文に記述しない．
例えば，
- 「指示に従って」
- 「読者の混乱を防ぐため」
- 「以前の指示では」
- 「トークン数を削減するため」
- 「ユーザーから指定されたため」
など，文書作成過程に関する説明は成果物に含めない．
成果物は，その文書を読む利用者にとって独立した文書として成立するように記述する．

## 1.4 共通指示
- 日本語の文章における句読点には必ずカンマ「，」とピリオド「．」を使用する．
- 文章は「である調」を用い論文を意識して記述する．
- 文章を書く様に頻繁に改行を挟まないでください．論文では大きく意味が変わらない限り改行を挟むべきではないです．
- `.cls` ファイルなどを編集しフォーマットを変更しないでください
- 本文中で略称を記述する際は，初めて略称を記述する際に，正式名称(略称)と記述する．
- 特に和名と英名があり，略称がアルファベットで記述される時は，和名(英名，略称)

例)
```text
Nesterov Accelerated Gradient Method (NAG)
確率的勾配降下法 (Stochastic Gradient Descent，SGD)
ニューラルネットワーク (Neural Networks，NN)
```
  
- 図のキャプション（\caption）は図の下，表のキャプションは表の上に配置することを徹底する． 
- figure環境やtabular環境で図や表を記述する際は，適時インデントを加えソースの可読性を担保してください．
- 式，図，表は下記のルールに従いラベルを命名する

| 対象 | 記法の例 |
| :--- | :--- |
| 式 | eq_{式の概要} |
| 図 | fig_{図の概要} |
| 表 | tab_{表の概要} | 

### 数式の記述方法
- 数式は必ずTeX表記（equation，align，$ inline $）を使用し，式中の変数は必ず説明する．
- equation環境やalign環境で数式を記述する際は，適時インデントを加えソースの可読性を担保してください．
- $ inline $ の中で数式を記述する際は，$ (ドルマーク)の前後で空白を加えソースの可読性を担保してください．
- 本文中に登場するハイパーパラメータや変数の記号（例: \alpha, \eta, k）は，文章中であっても必ず数式モード（$ inline $）で記述してください．
- 数学的対象を記述する際は下記のルールに従い記述してください
  
| 数学的対象 | ルール | 記法の例 |
| :--- | :--- | :--- |
| スカラー | 小文字・斜体 | x |
| ベクトル | 小文字・太字・非斜体 | {\mathbf x} | 
| 行列・テンソル | 大文字・太字・非斜体 | {\mathbf X} |
| 確率分布 | 大文字・スクリプト体 | {\mathcal N} |
| 集合 | 大文字・中抜き | {\mathbb X} |
| 関数 | 小文字・斜体 | f |

例) 
```text
全結合層を(\ref{eq_Dense})に示す．

\begin{equation}
  {\mathbf y}_i = f_i ( {\mathbf y}_{i - 1} {\mathbf W}_i + {\mathbf b}_i )
  \label{eq_Dense}
\end{equation}

\noindent
ここで$ {\mathbf y}_i \in {\mathbb R}^{1 \times D_i} $が$ i $層目の全結合層の出力，$ {\mathbf W}_i \in {\mathbb R}^{D_{i - 1} \times D_i} $が$ i $層目の全結合層の重み行列，$ {\mathbf b}_i \in {\mathbb R}^{1 \times D_i} $が$ i $層目の全結合層のしきい値ベクトル，$ f_i: {\mathbb R}^{1 \times D_i} \to {\mathbb R}^{1 \times D_i} $が$ i $層目の活性化関数，$ D_i $が$ i $層目の全結合層のニューロン数である．
```

# 2. report.md に関する規約
- プログラムの実行が終了した後に，これまでに作成したプログラムの内容，実験条件，実験結果および考察を `.reports/report.md` にまとめる．
- 作成するプログラムの指示書に通し番号が付いている場合は， `.orders/order_{k}.md` に対応して， `.reports/report_{k}.md` というファイル名でレポートを作成する．
-`report.md` は学術論文ではなく，実施したプログラムや実験の記録として作成する．そのため，論文向けの表現に過度に変更せず，実際に実行したプログラム，実験名，実験条件，生成された結果等との整合性を維持する．
- 実験結果が存在する場合は，必ず生成したグラフや表を参照しながら，結果および考察を詳細に記述する．
- 実験を実施していない場合，実施していない実験結果を推測して記述しない．


# 3. document.md に関する規約
`document.md` には，プロジェクト内で作成されたプログラムおよび文書の役割，依存関係，実行方法，セットアップ方法等を記述する．
少なくとも以下の内容を必要に応じて記述する．
- 各ディレクトリおよびファイルの役割
- プログラム間の依存関係
- 外部モジュールとの依存関係
- Python環境の構築方法
- プログラムの実行方法
- 実験結果の保存場所
- 文書およびレポートの保存場所
- 必要なAPIキーや設定ファイル
- Git管理上の注意事項
プログラムの実装方法そのものを過度に詳細に記述するのではなく，プロジェクト全体を理解し，再現するために必要な情報を記述する．

# 4. 学術論文作成に関する規約
LaTeX（TeX）を用いて和文および英文の学術論文，ジャーナル投稿原稿等を作成・改訂する場合は，以下の規約を適用する．
TeX文書をコンパイルする際は，SyncTeXを有効にしてソースとPDFの相互参照を可能にする．
TeX文書の作成が依頼された場合，その環境にはTeX-Live-Fullがインストールされており，下記のコマンドでコンパイルできます．
```text
latexmk -pdfdvi -synctex=1 main.tex
latexmk -C
```

- TeXレポートはAIエディターの終了時に作成する `report.md` とは別物で，人間からの指示があった場合にのみ作成してください
- TeXレポートの作成が依頼された場合，`@.ai/ai-tex-kit/templates/` 内のTeXファイル等をプロジェクトのルートにコピーし，これを編集してPDFのレポートを作成してください
- この際，フォルダ名はレポートの内容に応じ適時変更してください
- TeXのソースは `section` ごとに記述し，これを `main.tex` から参照させてください．
- TeXのソースは1行ごとに改行し，ソースを編集する際の可読性を高めてください．


## 4.1 文章・表記
### 4.1.1 句読点・数式記号
指定された執筆フォーマットに従い，句読点，記号，数式記号等の表記を文書全体で統一する．
和文論文においてコンマ・ピリオド表記が指定されている場合は，全ての文章で統一する．

### 4.1.2 自然な日本語表現
名詞を接続詞なしで過剰に連結した不自然な表現を避ける．
例えば，
- 「勾配評価点」
- 「学習率候補」
- 「定数学習率」
のような表現は，文脈に応じて，
- 「勾配の評価点」
- 「学習率の候補」
- 「定数の学習率」
など，意味が明確になるように書き換える．
文章として自然な日本語になるよう，「の」「における」「に対する」等の接続を適切に用いる．
論文らしい改行の仕方を意識してください．大きく意味の変わる部分以外に改行は不要です．

### 4.1.3 主語と述語
将来的な英語翻訳を考慮し，主語と述語の関係を明確にする．
主語を過度に省略したり，「これ」「それ」等の指示語を過剰に使用したりしない．
一つの文に複数の論理関係を詰め込みすぎず，必要に応じて文を分割する．

## 4.2 専門用語・概念の説明
### 4.2.1 初出時の説明
専門用語であっても，読者がその意味や導入する理由を理解できるよう，初出時に必要十分な説明を行う．
特に，分野によって意味が異なる用語や，論文内で独自の意味を持つ用語については，単に名称を示すだけでなく，その概念が何を表すのかを説明する．

### 4.2.2 数式による定義
アルゴリズムや数学的概念については，可能な場合には文章による説明だけでなく，数式を用いて定義する．
比較表等においても，「大きい」「小さい」「平均する」などの定性的な説明だけに依存せず，可能な限り具体的な数式で構造を示す．

### 4.2.3 前提条件
理論解析を行う場合，パラメータ，インデックス，ミニバッチ等の前提を明確に定義する．
例えば，イテレーション番号やエポック番号，ミニバッチを使用する場合は，それぞれに明確なインデックスを付与する．
理論解析上の仮定と実際の実装条件が異なる場合には，その違いと，理論解析においてその仮定を採用する理由を説明する．

# 5. 論文の構成
## 5.1 本文と付録
本文では，研究の目的，提案手法，仮定，主要な定理・補題の主張，結果およびそれらの意味を理解できるように記述する．
詳細な式変形や完全な証明については，必要に応じて付録へ移動する．
本文では，定理や補題からどのような結論が得られるのかを説明し，流し読みする読者でも研究の論理構造を把握できるようにする．
付録は参考文献の後に改ページして配置する．

## 5.2 定理・補題の番号
本文と付録で同一の定理・補題を扱う場合，番号を完全に一致させる．
LaTeXのカウンターを不用意にリセットせず，`\label` と `\ref` を用いて本文と付録の参照関係を管理する．
同一の主張や同等の式を異なる番号で重複して定義しない．

## 5.3 証明
付録で定理や補題の証明を行う場合，証明対象となる主張または主要な式を最初に示してから証明を開始する．
証明の終了位置には `\square` 等の証明完了記号を付ける．
「再掲」など，文書作成上の都合を示すメタ情報は記述しない．

# 6. TeX組版
## 6.1 数式
本文中で数式を参照する場合は，文書全体で統一された形式を使用する．
二段組の論文では，横長の数式が隣のカラムやページ端にはみ出さないよう，必要に応じて式を複数行に分割する．
不要な定義式や補足式に独立した式番号を付与しない．

## 6.2 表・アルゴリズム等
表，Algorithm，図等のフロート要素が互いに干渉しないように配置する．
必要に応じて配置指定や縦方向のスペースを調整し，本文や他のフロート要素との重なりを防止する．
アルゴリズムを表記する場合は，文書全体で `Algorithm` という表記を使用する．

## 6.3 定理環境
補題，定理等は `\newtheorem` 等の適切なLaTeX環境で管理する．
補題や定理を単なる節見出しとして表現しない．
定理環境に不要な斜体が適用される場合には，`\theoremstyle{definition}` 等を使用して読みやすい書体に調整する．

## 6.4 段落
1〜2行程度の不必要な改段落を避ける．
論理的に一つのまとまりを形成する内容は一つの段落として記述し，段落ごとに明確な役割を持たせる．

# 7. 実験・評価に関する記述
## 7.1 実験条件
実験を再現できるよう，必要な実験条件を具体的に記述する．
データセットについては，可能な限りデータ数 $N$，特徴量次元数 $d$，クラス数等を明記する．
モデルについては，モデル構造，目的関数，正則化項，バイアス項等の重要な条件を数式を用いて明確に記述する．

## 7.2 データセットの引用
公開データセットや既存手法を使用する場合は，原著論文，公式リポジトリ等の適切な文献を引用する．

## 7.3 比較手法
比較実験では，比較対象とする手法を明確に定義する．
各手法について，更新則，主要なハイパーパラメータ，その他の実験条件を可能な限り統一し，公平な比較となるようにする．

## 7.4 評価軸
グラフを用いて評価する場合，横軸と縦軸が何を意味するのかを本文中で説明する．
例えば，
- 計算コスト
- エポック数
- 実行時間
- 目的関数値または目的関数誤差
- 分類精度
- 勾配等の近似誤差
など，評価対象に応じて適切な指標を選択する．

# 8. 実験結果の扱い
実際に得られていない実験結果を推測して記述しない．
実験結果を記述する場合は，保存されたログ，グラフ，表等の実データと整合させる．
理論的に期待される結果と実験で観測された結果を混同せず，両者を明確に区別する．

# ディレクトリ構造の例
```text
./
├── .ai/ 			                # AIコーディングへの指示をまとめたディレクトリ
│   ├── ai-agent/ 		        # AIエージェントのプログラムのリポジトリ(サブモジュール)
│   │   ├── research/		      # 研究に関するAIエージェント
│   │   ├── development/	    # 開発に関するAIエージェント
│   │   ├── documentation/	  # 文書作成に関するAIエージェント
│   │   └── slide/		        # スライド作成に関するAIエージェント
│   ├── ai-dev-kit/		        # プログラムの作成指示，便利関数のリポジトリ(サブモジュール)
│   │
│   ├── ai-tex-kit/		        # 文書の作成指示，テンプレートのリポジトリ(サブモジュール)
│   │   ├── ja_paper/		      # 和文のフォーマット
│   │   │   ├── main.tex                    # latexでコンパイルするソース
│   │   │   ├── sections/                   # 論文の章ごとに分けられたソースをまとめたディレクトリ
│   │   │   └── figures/                    # 論文の画像をまとめたフォルダ
│   │   └── en_paper/		      # 英文のフォーマット
│   └── ai-pptx-kit/		      # スライドの作成指示，テンプレートのリポジトリ(サブモジュール)
│
├── paper/                    # AIコーディングが作成した論文（ディレクトリ名は適時変更する）
│   ├── main.tex
│   ├── sections/
│   │   ├── 01_introduction.tex
│   │   ├── 02_related_work.tex
│   │   └── ...
│   └── figures/
│
├── .venv_{環境名}/		        # 仮想環境
├── .logs/			              # AIエージェントのログを保持するディレクトリ
├── .orders/		              # AIコーディングへの指示を保持するディレクトリ
│   ├── order_001.md          # 人間が作成したAIコーディングへの指示
│   └── order_002.md
│
├── .reports/		              # AIコーディングのレポートを保持するディレクトリ
│   ├── report_001.md         # AIコーディングが作成した実行結果のサマリー
│   └── report_002.md
│
├── programs/		              # AIコーディングが作成したプログラムを保持するディレクトリ
├── outputs/		              # AIコーディングが作成したプログラムの出力を保持するディレクトリ
├── .gitignore		            # tokens.jsonを追加する
├── document.md		            # リポジトリの説明が書かれたmdファイル
├── tokens.json		            # APIトークンが格納されたjsonファイル
└── requirements_{環境名}.txt	# Pythonのライブラリを記述したtxtファイル

```